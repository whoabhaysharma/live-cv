{
  "name": "@dynamic-cv/resume-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for serving PDFs from R2 storage",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
